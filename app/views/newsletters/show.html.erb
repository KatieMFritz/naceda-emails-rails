<% content_for :page_title do %>
  News from Naceda
<% end %>

<% content_for :teaser_text do %>
  <%= @newsletter.teaser_text %>
<% end %>

<% content_for :banner_image do %>
  https://naceda.memberclicks.net/assets/images/Email_Banners/naceda_news.jpg
<% end %>

<% content_for :banner_alt do %>
  NACEDA News
<% end %>

<% content_for :banner_title do %>
  <h1>News <span class="small">from</span> Naceda</h1>
  <p class="tagline">News You Can Use to Advance Community Prosperity</p>
<% end %>

<% if @newsletter.has_attribute?('naceda_news') %>
  <!-- STRIP: Begin NACEDA news -->
  <h2>NACEDA News</h2>
  <%= @newsletter.naceda_news %>
<% end %>

<% if @newsletter.has_attribute?('naceda_events') %>
  <!-- STRIP: Begin NACEDA Events Panel -->
  <table cellpadding="10" cellspacing="0" border="0" cellspacing="0" border="0" class="panel">
    <thead>
      <tr class="panel-head">
        <td>
          <h4>NACEDA Events</h4>
        </td>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <%= @newsletter.naceda_events %>
        </td>
      </tr>
    </tbody>
  </table>
  <!-- STRIP: End events panel -->
  <%= render 'layouts/email_hr' %>
<% end %>

<% if @newsletter.has_attribute?('federal_news') %>
  <!-- STRIP: Begin federal news -->
  <h2>Federal News</h2>
  <%= @newsletter.federal_news %>
<% end %>

<% if @newsletter.has_attribute?('federal_events') %>
  <!-- STRIP: Begin Federal Events Panel -->
  <table cellpadding="10" cellspacing="0" border="0" cellspacing="0" border="0" class="panel">
    <thead>
      <tr class="panel-head">
        <td>
          <h4>Federal Events</h4>
        </td>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <%= @newsletter.federal_events %>
        </td>
      </tr>
    </tbody>
  </table>
<% end %>

<%= render 'layouts/email_hr' %>

<% if @newsletter.has_attribute?('member_news') %>
  <!-- STRIP: Begin Member News -->
  <h2>Member News</h2>
  <%= @newsletter.member_news %>
<% end %>

<% if @newsletter.has_attribute?('member_events') %>
  <!-- STRIP: Begin Member Events Panel -->
  <table cellpadding="10" cellspacing="0" border="0" cellspacing="0" border="0" class="panel">
    <thead>
      <tr class="panel-head">
        <td>
          <h4>Member Events</h4>
        </td>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <%= @newsletter.member_events %>
        </td>
      </tr>
    </tbody>
  </table>
  <!-- STRIP: End panel -->
<% end %>

<%= render 'layouts/email_hr' %>

<% if @newsletter.has_attribute?('partner_news') %>
  <!-- STRIP: Begin Partner News -->
  <h2>Partner News</h2>
  <%= @newsletter.partner_news %>
<% end %>

<% if @newsletter.has_attribute?('parter_events') %>
  <!-- STRIP: Begin Partner Events Panel -->
  <table cellpadding="10" cellspacing="0" border="0" cellspacing="0" border="0" class="panel">
    <thead>
      <tr class="panel-head">
        <td>
          <h4>Partner Events</h4>
        </td>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <%= @newsletter.partner_events %>
        </td>
      </tr>
    </tbody>
  </table>
  <!-- STRIP: End panel -->
<% end %>

<%= render 'layouts/email_hr' %>

<% if @newsletter.has_attribute?('jobs') %>
  <h2>Job Postings</h2>
  <%= @newsletter.jobs %>
  <p><em>To post a job opportunity, contact <a href="mailto:sgunther@naceda.org">Suzanne Gunther</a>. A $25 donation is suggested.</em></p>
  <%= render 'layouts/email_hr' %>
<% end %>

<h2>Funding Opportunities</h2>
<p>Go to our website for current grant opportunities. The're updated throughout the month.</p>
<%= render 'layouts/email_button', url: "http://naceda.org/funding-opportunities", text: "View Funding Opportunities" %>
